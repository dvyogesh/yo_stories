import styled from 'styled-components';

const ButtonWarapper = styled.div(()=>`
    button {
        border: 1px soild;
        background:none;
        padding:0 12px;
    }
`);

export default ButtonWarapper;